[network]
type = internal/network

; Name of the network interface to display. You can get the names of the
; interfaces on your machine with `ip link`
; Wireless interfaces often start with `wl` and ethernet interface with `eno` or `eth`
;interface = wlan1

; If no interface is specified, polybar can detect an interface of the given type.
; If multiple are found, it will prefer running interfaces and otherwise just
; use the first one found.
; Either 'wired' or 'wireless'
;interface-type = wireless

; Seconds to sleep between updates
; Default: 1
interval = 5

; NOTE: Experimental (might change or be removed in the future)
; Test connectivity every Nth update by pinging 8.8.8.8
; In case the ping fails 'format-packetloss' is used until the next ping
; A value of 0 disables the feature
; Default: 0
;ping-interval = 3

; Accumulate values from all interfaces
; when querying for up/downspeed rate
; Default: false
;accumulate-stats = true

; Consider an `UNKNOWN` interface state as up.
; Some devices like USB network adapters have
; an unknown state, even when they're running
; Default: false
;unknown-as-up = true

; The unit used for displaying network speeds
; For example if set to the empty string, a speed of 5 KB/s is displayed as 5 K
; Default: B/s
;speed-unit = ''

; Available tags:
;   <label-connected> (default)
;   <ramp-signal>
;format-connected = <ramp-signal> <label-connected>

; Available tags:
;   <label-disconnected> (default)
format-disconnected = <label-disconnected>
format-disconnected-background = ${color.status-negative}
format-disconnected-padding = ${label.padding}

; Used when connected, but ping fails (see ping-interval)
; Available tags:
;   <label-connected> (default)
;   <label-packetloss>
;   <animation-packetloss>
;format-packetloss = <animation-packetloss> <label-connected>

; All labels support the following tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %netspeed%  [wireless+wired] (%upspeed% + %downspeed%)
;   %linkspeed% [wired]
;   %mac%       [wireless+wired] (New in version 3.6.0)

; Default: %ifname% %local_ip%
;label-connected = %essid% %downspeed:9%

; Default: (none)
label-disconnected = ${label.off}
label-disconnected-alignment = center

; Default: (none)
;label-packetloss = %essid%

; Only applies if <ramp-signal> is used
;ramp-signal-0 = 😱
;ramp-signal-1 = 😠
;ramp-signal-2 = 😒
;ramp-signal-3 = 😊
;ramp-signal-4 = 😃
;ramp-signal-5 = 😈

; Only applies if <animation-packetloss> is used
;animation-packetloss-0 = ⚠
;animation-packetloss-1 = 📶
; Framerate in milliseconds
;animation-packetloss-framerate = 500
;===============================================================================
[xkeyboard]
type = internal/xkeyboard

; List of indicators to ignore
;blacklist-0 = num lock
;blacklist-1 = scroll lock

; Available tags:
;   <label-layout> (default)
;   <label-indicator> (default)
format = <label-indicator>

; Available tokens:
;   %layout%
;   %name%
;   %number%
;   %icon%
;   %variant%
; Default: %layout%
;label-layout = %name%
;label-layout-padding = 2
;label-layout-background = #bc99ed
;label-layout-foreground = #000

; layout-icon-[0-9]+ = layout;icon
; layout-icon-[0-9]+ = layout;variant;icon
; Assign each layout an icon that will be available as %icon% token for the
; <label-layout> tag.
; In the first configuration form, the `layout` will try to match %layout%
; value ignoring the variant
; In the second configuration form,
;     the `layout` will try to match %layout% value and the `variant` matches
;                  %variant%.
;     the `variant` can be empty (layout;;icon) to match against a %layout%
;                   with an empty %variant%
;     the `variant` can be the wildcard '_' to match a %layout% and any variant.
;         This is equivalent to the first configuration form
;     the `layout` cannot be empty (;variant;icon)
;     the `layout` can be the wildcard '_' to match any %layout% with a
;                  specific %variant%
;     Do not use the wildcard '_' for both `layout` and `variant`
;layout-icon-default = some-icon
;layout-icon-0 = ch;C
;layout-icon-1 = us;U

; indicator-icon-[0-9]+ = indicator;off icon;on icon
; Provides the content of the %icon% token in label-indicator-*
;indicator-icon-default =
;indicator-icon-0 = caps lock;-CL;+CL
;indicator-icon-1 = scroll lock;;+SL
;indicator-icon-2 = num lock;-NL;+NL

; Replaces label-indicator
; Available tokens:
;   %name%
;   %icon%
; Default: %name%
label-indicator-on = %icon%
label-indicator-on-background = ${color.focused-background}
label-indicator-on-foreground = ${color.focused-foreground}
label-indicator-on-padding = ${label.padding}

; Available tokens:
;   %name%
;   %icon%
; Default: (none)
label-indicator-off = %icon%
label-indicator-off-background = ${color.unfocused-background}
label-indicator-off-foreground = ${color.unfocused-foreground}
label-indicator-off-padding = ${label.padding}

; Replaces label-indicator-* if for specific indicators
; Available tokens:
;   %name%
;   %icon%
;label-indicator-on-capslock =
;label-indicator-off-capslock =
;label-indicator-on-numlock =
;label-indicator-off-numlock =
;label-indicator-on-scrolllock =
;label-indicator-off-scrolllock =
;===============================================================================
[module/backlight]
type = internal/backlight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
; Default: first usable card in /sys/class/backlight
;card = intel_backlight

; Use the `/sys/class/backlight/.../actual-brightness` file
; rather than the regular `brightness` file.
; Default: true
;use-actual-brightness = true

; Interval in seconds after which after which the current brightness is read
; (even if no update is detected).
; Use this as a fallback if brightness updates are not registering in polybar
; (which happens if the use-actual-brightness is false).
; There is no guarantee on the precisio of this timing.
; Set to 0 to turn off
; Default: 0 (5 if use-actual-brightness is false)
;poll-interval = 0

; Enable changing the backlight with the scroll wheel
; NOTE: This may require additional configuration on some systems. Polybar will
; write to `/sys/class/backlight/${self.card}/brightness` which requires polybar
; to have write access to that file.
; DO NOT RUN POLYBAR AS ROOT.
; The recommended way is to add the user to the
; `video` group and give that group write-privileges for the `brightness` file.
; See the ArchWiki for more information:
; https://wiki.archlinux.org/index.php/Backlight#ACPI
; Default: false
;enable-scroll = true

; Interval for changing the brightness (in percentage points).
; Default: 5
;scroll-interval = 10

; Available tags:
;   <label> (default)
;   <ramp>
;   <bar>
format = <ramp> <label>
format-padding = ${label.padding}

; Available tokens:
;   %percentage% (default)
label = %percentage:3%%

; Only applies if <ramp> is used
ramp-0 = 󰃚
ramp-1 = 󰃛
ramp-2 = 󰃜
ramp-3 = 󰃝
ramp-4 = 󰃞
ramp-5 = 󰃟
ramp-6 = 󰃠

; Only applies if <bar> is used
;bar-width = 10
;bar-indicator = |
;bar-fill = ─
;bar-empty = ─
;===============================================================================
[module/battery]
type = internal/battery

; This is useful in case the battery never reports 100% charge
; Default: 100
;full-at = 99

; format-low once this charge percentage is reached
; Default: 10
low-at = 25

; Use the following command to list batteries and adapters:
; $ ls -1 /sys/class/power_supply/
battery = BAT0
adapter = AC0

; If an inotify event haven't been reported in this many
; seconds, manually poll for new values.
;
; Needed as a fallback for systems that don't report events
; on sysfs/procfs.
;
; Disable polling by setting the interval to 0.
;
; Default: 5
;poll-interval = 5

; see "man date" for details on how to format the time string
; NOTE: if you want to use syntax tags here you need to use %%{...}
; Default: %H:%M:%S
;time-format = %H:%M

; Available tags:
;   <label-charging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-charging>
format-charging = <ramp-capacity>󱐋<label-charging>
format-charging-padding = ${label.padding}

; Available tags:
;   <label-discharging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-discharging>
format-discharging = <ramp-capacity> <label-discharging>
format-discharging-padding = ${label.padding}

; Available tags:
;   <label-full> (default)
;   <bar-capacity>
;   <ramp-capacity>
format-full = <ramp-capacity> <label-full>
format-full-padding = ${label.padding}

; Format used when battery level drops to low-at
; If not defined, format-discharging is used instead.
; Available tags:
;   <label-low>
;   <animation-low>
;   <bar-capacity>
;   <ramp-capacity>
format-low = <ramp-capacity> <label-low>
format-low-background = ${color.status-negative}
format-low-padding = ${label.padding}

; Available tokens:
;   %percentage% (default) - is set to 100 if full-at is reached
;   %percentage_raw%
;   %time%
;   %consumption% (shows current charge rate in watts)
label-charging = %percentage:3%%

; Available tokens:
;   %percentage% (default) - is set to 100 if full-at is reached
;   %percentage_raw%
;   %time%
;   %consumption% (shows current discharge rate in watts)
label-discharging = %percentage:3%%

; Available tokens:
;   %percentage% (default) - is set to 100 if full-at is reached
;   %percentage_raw%
label-full = %percentage:3%%

; Available tokens:
;   %percentage% (default) - is set to 100 if full-at is reached
;   %percentage_raw%
;   %time%
;   %consumption% (shows current discharge rate in watts)
label-low = %percentage:3%%

; Only applies if <ramp-capacity> is used
ramp-capacity-0 = 󰂎
ramp-capacity-1 = 󰁻
ramp-capacity-2 = 󰁽
ramp-capacity-3 = 󰁿
ramp-capacity-4 = 󰂁
ramp-capacity-5 = 󰁹

; Only applies if <bar-capacity> is used
;bar-capacity-width = 10

; Only applies if <animation-charging> is used
;animation-charging-0 = 
;animation-charging-1 = 
;animation-charging-2 = 
;animation-charging-3 = 
;animation-charging-4 = 
; Framerate in milliseconds
;animation-charging-framerate = 750

; Only applies if <animation-discharging> is used
;animation-discharging-0 = 
;animation-discharging-1 = 
;animation-discharging-2 = 
;animation-discharging-3 = 
;animation-discharging-4 = 
; Framerate in milliseconds
;animation-discharging-framerate = 500

; Only applies if <animation-low> is used
;animation-low-0 = !
;animation-low-1 =
;animation-low-framerate = 200
;===============================================================================
[module/caps-lock]
inherit = xkeyboard

blacklist-0 = num lock
blacklist-1 = scroll lock

indicator-icon-0 = caps lock;󰪛;󰪛
;===============================================================================
[module/cpu]
type = internal/cpu

; Seconds to sleep between updates
; Default: 1
interval = 5

; Default: 80
;warn-percentage =

; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
format =  <label>
format-padding = ${label.padding}

; Format used when average CPU load (same as %percentage%) reaches
; warn-percentage
; If not defined, format is used instead.
; Available tags:
;   <label-warn>
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
format-warn =  <label-warn>
format-warn-background = ${color.status-negative}
format-warn-padding = ${label.padding}

; Available tokens:
;   %percentage% (default) - total cpu load averaged over all cores
;   %percentage-sum% - Cumulative load on all cores
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
label = %percentage:3%%

; Available tokens:
;   %percentage% (default) - total cpu load averaged over all cores
;   %percentage-sum% - Cumulative load on all cores
;   %percentage-cores% - load percentage for each core
;   %percentage-core[1-9]% - load percentage for specific core
label-warn = %percentage:3%%

; Only applies if <ramp-coreload> is used
; Spacing (number of spaces, pixels, points) between individual per-core ramps
;ramp-coreload-spacing = 1
;ramp-coreload-0 = ▁
;ramp-coreload-1 = ▂
;ramp-coreload-2 = ▃
;ramp-coreload-3 = ▄
;ramp-coreload-4 = ▅
;ramp-coreload-5 = ▆
;ramp-coreload-6 = ▇
;ramp-coreload-7 = █

; Only applies if <ramp-load> is used
ramp-load-0 = ▁
ramp-load-1 = ▂
ramp-load-2 = ▃
ramp-load-3 = ▄
ramp-load-4 = ▅
ramp-load-5 = ▆
ramp-load-6 = ▇
;===============================================================================
[module/date]
type = internal/date

; Seconds to sleep between updates
; Default: 1.0
;interval = 1.0

; See "https://en.cppreference.com/w/cpp/io/manip/put_time" for details on how
; to format the date string
; NOTE: if you want to use syntax tags here you need to use %%{...}
date = %d/%m/%Y

; Optional time format
time = %H:%M:%S

; if `date-alt` or `time-alt` is defined, clicking
; the module will toggle between formats
;date-alt = %A, %d %B %Y
;time-alt = %H:%M:%S

; Available tags:
;   <label> (default)
format = <label>
format-padding = ${label.padding}

; Available tokens:
;   %date%
;   %time%
; Default: %date%
label = 󰸗 %date% 󰥔 %time%
label-minlen = ${size.date-label-minlen}
label-maxlen = ${size.date-label-maxlen}
label-alignment = center
;===============================================================================
[module/ethernet]
inherit = network

; If no interface is specified, polybar can detect an interface of the given
; type.
; If multiple are found, it will prefer running interfaces and otherwise just
; use the first one found.
; Either 'wired' or 'wireless'
interface-type = wired

; Available tags:
;   <label-connected> (default)
;   <ramp-signal>
format-connected = 󰈀 <label-connected>
format-connected-padding = ${label.padding}

; Available tags:
;   <label-disconnected> (default)
format-disconnected-prefix = "󰈀 "

; All labels support the following tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %netspeed%  [wireless+wired] (%upspeed% + %downspeed%)
;   %linkspeed% [wired]
;   %mac%       [wireless+wired]
; Default: %ifname% %local_ip%
label-connected = %linkspeed%
label-connected-minlen = ${size.ethernet-label-connected-minlen}
label-connected-maxlen = ${size.ethernet-label-connected-maxlen}
label-connected-alignment = right

; Default: (none)
label-disconnected-minlen = ${size.ethernet-label-disconnected-minlen}
label-disconnected-maxlen = ${size.ethernet-label-disconnected-maxlen}
;===============================================================================
[module/filesystem]
type = internal/fs

; Mountpoints to display
; Default: /
;mount-0 = /
;mount-1 = /home
;mount-2 = /var

; Seconds to sleep between updates
; Default: 30
;interval = 10

; Display fixed precision values
; Default: false
;fixed-values = true

; Spacing (number of spaces, pixels, points) between entries
; Default: 2
;spacing = 4

; Default: 90
warn-percentage = 80

; Available tags:
;   <label-mounted> (default)
;   <bar-free>
;   <bar-used>
;   <ramp-capacity>
format-mounted = 󰋊 <label-mounted>
format-mounted-padding = ${label.padding}

; Available tags:
;   <label-unmounted> (default)
;format-unmounted = <label-unmounted>

; Format used when mountpoint %percentage_used% reaches warn-percentage
; If not defined, format-mounted is used instead.
; Available tags:
;   <label-warn>
;   <bar-free>
;   <bar-used>
;   <ramp-capacity>
format-warn = 󰋊 <label-warn>
format-warn-background = ${color.status-negative}
format-warn-padding = ${label.padding}

; Available tokens:
;   %mountpoint%
;   %type%
;   %fsname%
;   %percentage_free%
;   %percentage_used%
;   %total%
;   %free%
;   %used%
; Default: %mountpoint% %percentage_free%%
label-mounted = %percentage_used:3%%

; Available tokens:
;   %mountpoint%
; Default: %mountpoint% is not mounted
;label-unmounted = %mountpoint%: not mounted
;label-unmounted-foreground = #55

; Available tokens:
;   %mountpoint%
;   %type%
;   %fsname%
;   %percentage_free%
;   %percentage_used%
;   %total%
;   %free%
;   %used%
; Default: %mountpoint% %percentage_free%%
label-warn = %percentage_used:3%%

; Only applies if <ramp-capacity> is used
ramp-capacity-0 = ▇
ramp-capacity-1 = ▆
ramp-capacity-2 = ▅
ramp-capacity-3 = ▄
ramp-capacity-4 = ▃
ramp-capacity-5 = ▂
ramp-capacity-6 = ▁
;===============================================================================
[module/i3]
type = internal/i3

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; on different bars
;
; Default: false
;pin-workspaces = true

; Show urgent workspaces regardless of whether the workspace is actually hidden
; by pin-workspaces.
;
; Default: false
;show-urgent = true

; This will split the workspace name on ':'
; Default: false
;strip-wsnumbers = true

; Sort the workspaces by index instead of the default
; sorting that groups the workspaces by output
; Default: false
;index-sort = true

; Create click handler used to focus workspace
; Default: true
;enable-click = false

; Create scroll handlers used to cycle workspaces
; Default: true
enable-scroll = false

; Wrap around when reaching the first/last workspace
; Default: true
;wrapping-scroll = false

; Set the scroll cycle direction
; Default: true
;reverse-scroll = false

; Use fuzzy (partial) matching for wc-icon.
; Example: code;♚ will apply the icon to all workspaces
; containing 'code' in the name
; Default: false
;fuzzy-match = true

; ws-icon-[0-9]+ = <label>;<icon>
; NOTE: The <label> needs to match the name of the i3 workspace
; Neither <label> nor <icon> can contain a semicolon (;)
; NOTE: You cannot skip icons, e.g. to get a ws-icon-6
; you must also define a ws-icon-5.
; NOTE: Icon will be available as the %icon% token inside label-*
;ws-icon-0 = 1;♚
;ws-icon-1 = 2;♛
;ws-icon-2 = 3;♜
;ws-icon-3 = 4;♝
;ws-icon-4 = 5;♞
;ws-icon-default = ♟
; NOTE: You cannot skip icons, e.g. to get a ws-icon-6
; you must also define a ws-icon-5.
; NOTE: Icon will be available as the %icon% token inside label-*

; Available tags:
;   <label-state> (default) - gets replaced with
;                             <label-(focused|unfocused|visible|urgent)>
;   <label-mode> (default)
format = <label-state> <label-mode>

; Available tokens:
;   %mode%
; Default: %mode%
label-mode = %mode%

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon% %name%
label-focused = %name%
label-focused-foreground = ${color.focused-foreground}
label-focused-background = ${color.focused-background}
label-focused-padding = ${label.padding}

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon% %name%
label-unfocused = %name%
label-unfocused-foreground = ${color.unfocused-foreground}
label-unfocused-background = ${color.unfocused-background}
label-unfocused-padding = ${label.padding}

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon% %name%
label-visible = %name%
label-visible-foreground = ${color.visible-foreground}
label-visible-background = ${color.visible-background}
label-visible-padding = ${label.padding}

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon% %name%
label-urgent = %name%
label-urgent-foreground = ${color.urgent-foreground}
label-urgent-background = ${color.urgent-background}
label-urgent-padding = ${label.padding}

; Separator in between workspaces
label-separator = ${separator.character}
label-separator-padding = ${separator.padding}
label-separator-font = ${separator.font}
label-separator-background = ${separator.background}
;===============================================================================
[module/memory]
type = internal/memory

; Seconds to sleep between updates
; Default: 1
interval = 5

; Default: 90
;warn-percentage = 95
warn-percentage = 80

; Available tags:
;   <label> (default)
;   <bar-used>
;   <bar-free>
;   <ramp-used>
;   <ramp-free>
;   <bar-swap-used>
;   <bar-swap-free>
;   <ramp-swap-used>
;   <ramp-swap-free>
format =  <label>
format-padding = ${label.padding}

; Format used when RAM reaches warn-percentage
; If not defined, format is used instead.
; Available tags:
;   <label-warn>
;   <bar-used>
;   <bar-free>
;   <ramp-used>
;   <ramp-free>
;   <bar-swap-used>
;   <bar-swap-free>
;   <ramp-swap-used>
;   <ramp-swap-free>
format-warn =  <label-warn>
format-warn-background = ${color.status-negative}
format-warn-padding = ${label.padding}

; Available tokens:
;   %percentage_used% (default)
;   %percentage_free%
;   %used% (Switches between KiB, MiB, and GiB)
;   %free% (Switches between KiB, MiB, and GiB)
;   %total% (Switches between KiB, MiB, and GiB)
;   %gb_used%
;   %gb_free%
;   %gb_total%
;   %mb_used%
;   %mb_free%
;   %mb_total%
;   %percentage_swap_used%
;   %percentage_swap_free%
;   %swap_total% (Switches between KiB, MiB, and GiB)
;   %swap_free% (Switches between KiB, MiB, and GiB)
;   %swap_used% (Switches between KiB, MiB, and GiB)
;   %gb_swap_total%
;   %gb_swap_free%
;   %gb_swap_used%
;   %mb_swap_total%
;   %mb_swap_free%
;   %mb_swap_used%
label = %percentage_used:3%%

; Available tokens:
;   %percentage_used% (default)
;   %percentage_free%
;   %used% (Switches between KiB, MiB, and GiB)
;   %free% (Switches between KiB, MiB, and GiB)
;   %total% (Switches between KiB, MiB, and GiB)
;   %gb_used%
;   %gb_free%
;   %gb_total%
;   %mb_used%
;   %mb_free%
;   %mb_total%
;   %percentage_swap_used%
;   %percentage_swap_free%
;   %swap_total% (Switches between KiB, MiB, and GiB)
;   %swap_free% (Switches between KiB, MiB, and GiB)
;   %swap_used% (Switches between KiB, MiB, and GiB)
;   %gb_swap_total%
;   %gb_swap_free%
;   %gb_swap_used%
;   %mb_swap_total%
;   %mb_swap_free%
;   %mb_swap_used%
label-warn = %percentage_used:3%%

; Only applies if <bar-used> is used
;bar-used-indicator =
;bar-used-width = 50
;bar-used-foreground-0 = #55aa55
;bar-used-fill = ▐
;bar-used-empty = ▐
;bar-used-empty-foreground = #444444

; Only applies if <ramp-used> is used
;ramp-used-0 = ▁
;ramp-used-1 = ▂
;ramp-used-2 = ▃
;ramp-used-3 = ▄
;ramp-used-4 = ▅
;ramp-used-5 = ▆
;ramp-used-6 = ▇

; Only applies if <ramp-free> is used
;ramp-free-0 = ▁
;ramp-free-1 = ▂
;ramp-free-2 = ▃
;ramp-free-3 = ▄
;ramp-free-4 = ▅
;ramp-free-5 = ▆
;ramp-free-6 = ▇
;ramp-free-7 = █
;===============================================================================
[module/num-lock]
inherit = xkeyboard

blacklist-0 = caps lock
blacklist-1 = scroll lock

indicator-icon-0 = num lock;󰎠;󰎠
;===============================================================================
[module/pulseaudio]
type = internal/pulseaudio

; Sink to be used, if it exists (find using `pacmd list-sinks`, name field)
; If not, uses default sink
; sink = alsa_output.pci-0000_12_00.3.analog-stereo

; Use PA_VOLUME_UI_MAX (~153%) if true, or PA_VOLUME_NORM (100%) if false
; Default: true
use-ui-max = false

; Interval for volume increase/decrease (in percent points)
; Default: 5
;interval = 5

; Reverses the increment/decrement on scroll event. Set this to true if you are
; using natural scrolling option on your touchpad.
; Default: false
;reverse-scroll = false

; Available tags:
;   <label-volume> (default)
;   <ramp-voluME>
;   <bar-volume>
format-volume = <ramp-volume> <label-volume>
format-volume-padding = ${label.padding}

; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
format-muted =  <label-muted>
format-muted-background = ${color.status-negative}
format-muted-padding = ${label.padding}

; Available tokens:
;   %percentage% (default)
;   %decibels%
label-volume = %percentage:3%%

; Available tokens:
;   %percentage% (default)
;   %decibels%
label-muted = %percentage:3%%

; Only applies if <ramp-volume> is used
ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 

; Right and Middle click
;click-right =
;click-middle =
;===============================================================================
[module/temperature]
type = internal/temperature

; Seconds to sleep between updates
; Default: 1
interval = 10

; Thermal zone to use
; To list all the zone types, run
; $ for i in /sys/class/thermal/thermal_zone*; do echo "$i: $(<$i/type)"; done
; Default: 0
;thermal-zone = 0

; Select thermal zone by name
; The name has to match the contents of /sys/class/thermal/thermal_zone*/type
; for the desired thermal zone.
; Default: ""
zone-type = acpitz

; Full path of temperature sysfs path
; Use `sensors` to find preferred temperature source, then run
; $ for i in /sys/class/hwmon/hwmon*/temp*_input; do echo "$(<$(dirname $i)/name): $(cat ${i%_*}_label 2>/dev/null || echo $(basename ${i%_*})) $(readlink -f $i)"; done
; to find path to desired file
; Default reverts to thermal zone setting
;hwmon-path = /sys/devices/platform/coretemp.0/hwmon/hwmon2/temp1_input

; Base temperature for where to start the ramp (in degrees celsius)
; Default: 0
base-temperature = 20

; Threshold temperature to display warning label (in degrees celsius)
; Default: 80
;warn-temperature = 60

; Whether or not to show units next to the temperature tokens (°C, °F)
; Default: true
;units = false

; Available tags:
;   <label> (default)
;   <ramp>
format = <ramp> <label>
format-padding = ${label.padding}

; Available tags:
;   <label-warn> (default)
;   <ramp>
format-warn = <ramp> <label-warn>
format-warn-background = ${color.status-negative}
format-warn-padding = ${label.padding}

; Available tokens:
;   %temperature-c%   (default, temperature in °C)
;   %temperature-f%   (temperature in °F)
;   %temperature-k%   (temperature in Kelvin)
label = %temperature-c:5%

; Available tokens:
;   %temperature-c%   (default, temperature in °C)
;   %temperature-f%   (temperature in °F)
;   %temperature-k%   (temperature in Kelvin)
label-warn = %temperature-c:5%

; Requires the <ramp> tag
; The icon selection will range from `base-temperature` to `warn-temperature`,
; temperatures at and above `warn-temperature` will use the last icon
; and temperatures at and below `base-temperature` will use `ramp-0`.
; All other icons are distributed evenly between the two temperatures.
ramp-0 = 
ramp-1 = 
ramp-2 = 
ramp-3 = 
ramp-4 = 
;===============================================================================
[module/tray]
type = internal/tray

; Available Tags:
;   <tray>: Shows tray icons
; Default Value: <tray>
format = <tray>
format-padding = ${label.padding}

; Space added between tray icons
; Default Value: 0px
tray-spacing = 10px

; Space added before and after each tray icon
; Default Value: 0px
;tray-padding = 0px

; Size of individual tray icons
; Default Value: 66%
;tray-size = 66%

; Background color of tray icons
; This only affects the color of the individual icons and not the space in
; between, changing this setting to anything else than the bar background will
; likely not look good unless the background color is also changed for the rest
; of the tray module (e.g. with format-background)
; Default Value: ${root.background}
;tray-background = ${root.background}

; Tray icon color
; This serves as a hint to the tray icon application for which color to use for
; the icon
; This is not guaranteed to have any effect (likely only in GTK3) because it
; targets a non-standard part of the system tray protocol by setting the
; _NET_SYSTEM_TRAY_COLORS atom on the tray window
; Default Value: ${tray-foreground}
;tray-foreground = ${tray-foreground}
;===============================================================================
[module/wireless]
inherit = network

; If no interface is specified, polybar can detect an interface of the given
; type.
; If multiple are found, it will prefer running interfaces and otherwise just
; use the first one found.
; Either 'wired' or 'wireless'
interface-type = wireless

; Available tags:
;   <label-connected> (default)
;   <ramp-signal>
format-connected = 󰖩 <label-connected>
format-connected-padding = ${label.padding}

; Available tags:
;   <label-disconnected> (default)
format-disconnected-prefix = "󰖪 "

; All labels support the following tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %local_ip6% [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %netspeed%  [wireless+wired] (%upspeed% + %downspeed%)
;   %linkspeed% [wired]
;   %mac%       [wireless+wired]
; Default: %ifname% %local_ip%
label-connected = %essid%
label-connected-minlen = ${size.wifi-label-connected-minlen}
label-connected-maxlen = ${size.wifi-label-connected-maxlen}
label-connected-alignment = center

; Default: (none)
label-disconnected-minlen = ${size.wifi-label-disconnected-minlen}
label-disconnected-maxlen = ${size.wifi-label-disconnected-maxlen}

; Only applies if <ramp-signal> is used
;ramp-signal-0 = 󰣾
;ramp-signal-1 = 󰣴
;ramp-signal-2 = 󰣶
;ramp-signal-3 = 󰣸
;ramp-signal-4 = 󰣺
;===============================================================================
[module/xwindow]
type = internal/xwindow

; Available tags:
;   <label> (default)
format = <label>
format-background = ${color.focused-background}
format-foreground = ${color.focused-foreground}
format-padding = ${label.padding}

; Available tokens:
;   %title%
;   %instance% (first part of the WM_CLASS atom)
;   %class%    (second part of the WM_CLASS atom)
; Default: %title%
label = %title%
label-minlen = ${size.xwindow-label-minlen}
label-maxlen = ${size.xwindow-label-minlen}
label-alignment = center

; Used instead of label when there is no window title
; Available tokens:
;   None
label-empty = ${label.off}
label-empty-minlen = ${size.xwindow-label-minlen}
label-empty-maxlen = ${size.xwindow-label-maxlen}
label-empty-alignment = center
