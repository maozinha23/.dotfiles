exec swayidle -w \
  timeout 600 'swaymsg "output * power off"' \
    resume 'swaymsg "output * power on" && swaylock -f' \
  before-sleep 'swaylock -f'

# Status bar
bar {
  swaybar_command waybar
}

# Removable disk mounter
exec udiskie
